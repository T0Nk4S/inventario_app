# Este archivo contiene las dependencias de tu proyecto Flutter.

name: inventario_app
description: Sistema de Gestión de Inventario en Tiempo Real.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+2

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # External dependency removed — backend replaced by local stub or Firestore adapter.
  
  # Utilidad para generar IDs universales únicas (UUIDs) para idProducto, idVenta, etc.
  uuid: ^4.3.3
  http: ^1.1.0
  
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # Internacionalización / formateo de fechas
  intl: ^0.20.2
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1
  mobile_scanner: ^7.1.2
  shared_preferences: ^2.1.0
  pdf: ^3.10.1
  printing: ^5.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
